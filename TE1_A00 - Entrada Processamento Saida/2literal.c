#include <stdio.h>

int main()
{

    // ENTRADA
    int firstNumber = 50;
    int secondNumber = 50;

    // PROCESSAMENTO
    int soma = firstNumber + secondNumber;

    // SAIDA
    printf("Soma = %i", soma);

    return 0;
}